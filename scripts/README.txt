These are custom scripts (BASH, R, Perl, Python, etc) that are used as 
commands in the pipeline.
They are automaticaly added to the pipeline execution PATH.
NOTE: All scripts should have the execute permission
